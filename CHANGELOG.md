# 변경 이력

날짜 기준으로 적는다. 각 항목 끝의 해시로 `git show <해시>` 하면 상세 내용이 나온다.

---

## 2026-09-23

### 더함

- **`stock_price`가 정규장 밖에서 정규장 종가를 따로 보여 준다.** 16시 이후엔 현재가도
  일봉 종가도 애프터마켓 가격이라, 저녁에 "오늘 종가"를 정확히 답할 방법이 없었다.
  분봉은 KRX 단독이라 15:30 이하 마지막 봉이 정규장 종가다 — 9/23 삼성전자 285,500원·
  SK하이닉스 1,862,000원으로 뉴스 종가와 일치(현재가는 286,500원·1,863,000원).
  `verify.py`가 이 방식을 네이버 기준가(현재가 − 전일대비 = 정규장 전일 종가)와 대조한다
  — 일봉 종가로 바꾸면 세 종목 모두 FAIL. 호출은 정규장 밖에서만 2회 늘어난다. `5087615`

- **`stock_compare`에 `sort_by`.** 선행PER·PER·PBR은 낮은 순, 시총·ROE(E)·OPM은 높은
  순이고 적자·값 없음·조회 실패는 맨 아래다. 리뷰에서 필터(`max_fwd_per`)도 제안했지만
  넣지 않았다 — 행을 숨기면 조회 실패 행을 남겨 누락을 알아채게 한 이유가 무너지고,
  50종목 표도 5,600자 정도라 줄일 이득이 작다. `5087615`

### 바꿈

- **`stock_detail`·`stock_investor_trend`에도 가격 기준을 적는다.** 종목 상세의 시가·
  고가·저가·거래량은 KRX+NXT 통합이고(9/23 삼성전자 저가 280,750원은 NXT 체결, KRX는
  281,000원) 시총·PER·PBR은 조회 시점 가격이다. 수급 표의 종가는 그날 마지막 체결가라
  애프터마켓이 있던 날은 애프터마켓 종가다(9/22 삼성전자 277,500원, 정규장 276,500원). `5087615`

- **적자면 PER·선행PER에 `적자`라고 쓴다.** 선행PER은 적자 추정이면 음수(롯데케미칼
  -14.43)로 나왔다. 음수 PER은 "10배 이하" 같은 필터를 숫자로는 통과하고, 크기
  순서도 뜻이 없다 — 카카오게임즈 -7.55가 엘앤에프 -80.25보다 적자가 크다. 후행
  PER은 네이버가 적자면 N/A를 줘서 `-`로 나왔고, 값 없음과 구분되지 않았다. 이제
  두 열 모두 적자면 `적자`, 값이 없으면 `-`이고, 적자 규모는 `EPS(E)` 열에서 본다.
  처음엔 선행PER만 `-`로 바꿨다가(`efb5ba8`) "데이터 없음"처럼 읽혀 `적자`로 정했다.
  `verify.py`가 두 열을 모두 확인한다 — 후행 PER은 종목 상세의 EPS와 대조하고,
  검사 종목에 적자 기업(롯데케미칼)을 더했다. `36ce6bb`

### 고침

- **현재가가 어느 시세인지 적는다.** 2026-09-14 KRX 애프터마켓(16:00~20:00 실시간
  매매)이 열린 뒤로 네이버 현재가는 정규장이 끝나도 20:00까지 움직이고, 20:00
  이후엔 애프터마켓 종가다 — 9/22 삼성전자는 정규장 종가 276,500원, 네이버 종가
  277,500원. 그런데 표는 전 종목이 `CLOSE`일 때만 "장 마감 → 현재가는 종가"를
  달았고, 애프터마켓 중엔 `marketStatus`가 `OPEN`이라 안내가 사라졌다(18:53 호출
  리뷰). 이제 `stock_price`·`stock_compare`가 늘 기준(정규장 실시간 / 애프터마켓
  실시간 / 장 마감 후 마지막 체결가)과 조회 시각을 적는다. ETF·ETN은 애프터마켓이
  없어 그 시간엔 따로 표시된다. `425d9b9`

- **`stock_compare`의 시총·PER·PBR이 현재가와 다른 가격 기준일 수 있었다.** 세 값을
  종목 상세에서 옮겨 왔는데 현재가와 요청이 따로라, 애프터마켓 중엔 시총 상위
  140종목 중 24개가 1~2틱(GS는 0.9%) 다른 가격 기준이었다 — 삼성전자가 같은 현재가
  285,500원에 시총 1,666조/1,669조로 갈린 원인이다. 이제 시세를 한 요청으로 받아
  모든 행이 같은 시점 가격이고, 시총은 그 응답의 값, PER·PBR은 현재가 ÷ EPS·BPS로
  계산한다(가격이 같을 때 네이버 값과 99/99·128/128 일치). ETF도 시총이 나온다.
  `stock_price`도 같은 시세 API로 바꿨다 — 전에 쓰던 API는 40초가량 늦었다.
  `verify.py`가 세 열을 모두 검산한다 — 가격을 1% 움직이면 옛 방식은 28칸에서 FAIL.
  `425d9b9`

- **50개를 넘겨 생략한 종목의 코드를 적는다.** "나머지 1개 생략"만 나와 51번째로
  넣은 삼성전자우가 빠진 걸 알 수 없었다. `425d9b9`

- **`stock_compare`의 `*` 선행PER이 현재가 기준이 아니었다.** 종목 상세에 추정치가
  없는 종목은 실적표 추정 열의 PER을 그대로 옮겨 왔는데, 그 PER은 현재가가 아닌
  배치 시점 값이다 — 시총 상위 200종목 중 추정치가 있는 161종목에서 158개가
  전일 종가 ÷ EPS(E)와 일치했다. 당일 등락만큼 틀려 스크리닝 경계를 뒤집는다.
  프로텍은 9.56으로 10배 필터를 통과했지만 현재가 기준으로는 10.18이었다.
  "소수점 단위로 어긋날 수 있다"던 주석은 틀린 설명이었고, 시총 상위 200종목 중
  32개(16%)가 이 경로를 탄다.

  이제 선행PER은 모든 행에서 **그 행에 찍힌 현재가 ÷ EPS(E)**다. 종목 상세의
  추정PER도 현재가 기준이지만 요청이 따로라 호가 하나쯤 어긋날 수 있어 쓰지 않는다.
  `EPS(E)` 열을 더해 표만 보고 검산할 수 있게 했고, 늘 추정치인 `ROE`는 `ROE(E)`로
  바꿨다. `verify.py`가 모든 행을 검산한다 — `*` 경로를 옛 방식으로 되돌리면
  이오테크닉스에서 FAIL이 난다. `3596b24`

---

## 2026-09-22

### 더함

- **`stock_compare`** — 여러 종목을 한 표로 비교한다(최대 50개). 현재가·시총·
  PER·선행PER·PBR·영업이익률(확정/추정)·ROE.

  40종목을 보려면 도구를 40번 불러야 했다. 이제 한 번이면 되고 **0.72초에
  3,667자**로 끝난다. 종목마다 basic·integration·finance 세 API를 스레드 풀로
  동시에 받는다.

  선행PER이 종목 상세에 없으면(이오테크닉스 등) 실적표의 추정 연도 PER로
  보완하고 `*`를 붙인다 — 두 출처는 기준 시점이 달라 소수점 단위로 어긋난다.
  상장폐지·합병 소멸 종목은 조용히 빠지지 않고 `조회 실패` 행으로 남는다.
  전수 스크리닝에서 40개 중 39개만 나오면 무엇이 빠졌는지 알 수 없기 때문이다.
  `595e09d`

- **`verify.py`** — 도구가 실제로 쓸 만한 값을 돌려주는지 확인한다.
  `python verify.py`, 의존성 설치 없이 돈다.

  코드가 꺼내 쓰는 응답 키를 네이버 실제 응답과 대조하고, 종목 10개로 호출해
  "어느 종목에서도 비는 항목"을 찾는다. 같은 검사를 OpenDART 서버에 돌렸을 때는
  결함이 쏟아졌지만 **여기서는 0건**이었다 — 참조 키 16개와 `stock_detail`
  라벨 18개가 전부 실재했고, `stock_compare` 표에 죽은 컬럼도 깨진 행도 없었다.
  `d679abe`

- **GitHub Actions** — push·PR과 매주 월요일에 자동 실행. 주간 실행이 핵심이다.
  코드를 건드리지 않아도 네이버가 응답을 바꾸면 표가 조용히 빌 수 있기 때문이다.

  실제 `mcp`·`starlette`를 설치해 `import server`까지 확인한다. `verify.py`는
  두 패키지를 스텁으로 갈아끼우므로 import 오류를 볼 수 없는데, `mcp` 2.x가
  `FastMCP`를 개명해 배포가 죽은 적이 있다(2026-09-16). 스텁만 돌렸다면 그때도
  초록불이었을 것이다. `cd810ae`

### 고침

- 뉴스 날짜가 `202609221911`처럼 붙어 나왔다. `2026-09-22 19:11`로 바꿨고,
  형식이 다른 값은 원본을 유지한다. `d679abe`

### 알아둘 것

`stock_news`의 `tit`·`office`·`dt` 폴백은 실제 응답에 없는 키라 실행되지 않는다.
해롭지 않아 그대로 뒀다.

---

## 2026-09-16

- **`mcp<2`로 고정.** 상한이 없어 재빌드 때 2.x가 딸려 왔고, 2.x가 `FastMCP`를
  `MCPServer`로 개명하면서 `mcp.server.fastmcp` 모듈이 사라져 배포가 죽었다.
  기능 변경과 무관한 시한폭탄이었고 첫 재빌드에서 터졌다. `16231a1`
- 투자자 수급(`stock_investor_trend`)·실적 추이(`stock_financials`) 도구 추가,
  `stock_detail`에 목표주가 컨센서스 노출. `1ae535f`

---

## 그 이전

원본(stdio 방식, `agent504330-ux/mcp-korean-stock`)을 claude.ai 커스텀 커넥터에서
쓸 수 있도록 Streamable HTTP로 개조. Render 배포.
